package com.test.designpartern.Singleton;

import java.io.IOException;
import java.util.Properties;

/**
 * @ Author     ：zhoule
 * @ Date       ：Created in 15:36 2020-04-04
 * @ Description：
 * @ Modified By：
 * @Version: $
 */
public class Singleton {
    /*public static final Singleton INSTANCE;
    private String info;


    static {
        try {
            Properties properties = new Properties();
            properties.load(Singleton.class.getClassLoader().getResourceAsStream(singleton.properties));
            INSTANCE = Singleton(properties.getProperty(key));
        } catch (IOException e) {
            e.printStackTrace();
        }
    }

    private Singleton(String info) {
        this.info = info;
    }*/
}
